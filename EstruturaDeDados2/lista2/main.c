// 1) O número de inversões de um vetor v[0 . . . n−1] é o número de pares ordenados (i, j) tais que 0 ≤ i < j < n
// e v[i] > v[j]. Modifique o algoritmo Merge Sort (e Merge) para calcular o número de inversões em v
#include <stdio.h>
int main(int argc, char const *argv[])
{
    printf("ola");
    return 0;
}
